# Change log: group incoming-paper-mail process notice found in Raw, verified

**2026-09-12**, while checking `Raw/` for outstanding items.

## What was found

A new group process notice: `2026-09-10_group-process_incoming-paper-mail-handling.md` (Drive id
`1yjN1ixv33uZB1GUnchnmwkUr6tScfd2i`, created 2026-09-10T15:44Z, 2,860 bytes). Unlike the three prior
Raw arrivals this session, this is not a document-numbering policy version — it describes a new
group process, `Wiki/Process-Post-Handling.md`, under which the group acts as a shared "post room"
for physical mail addressed to any of the seven group companies at the shared office. Once triaged,
a qualifying letter is registered in the group Document Register under the owning company's own
prefix and **routed into that company's KB `Raw/`** as a `§7a` hand-off (already-registered scan
plus a covering note) — the same mechanism adopted with policy v1.1/6a.

## What was checked

Independently verified against the group knowledge base before treating it as informational:

- The group KB's own live `current-state.md` (Drive id `1JMo14XotiF_n5XpUyKruycVyAlG3m9TW`,
  updated 2026-09-11T22:25Z) records the same rollout in its own words, including the first live
  run on 2026-09-10 (four letters registered and routed for Fishbone Waste and Fishbone
  Construction; none yet for this scheme).
- The group KB's own `00_INDEX.md` "Recently changed" section corroborates the same process.
- `Wiki/Process-Post-Handling.md` itself is on file in the group KB, now at v1.2 (intake path
  moved to `Raw/Paper Mail/`; Fishbone Waste routes to its own KB once created).

**Conclusion: genuine.**

## What this means for this KB

Nothing yet: no scan has been routed to this KB's `Raw/` under this process. When one does arrive,
it comes pre-registered (already carrying a group `FS0000nnn` document ID and a covering
`..._handoff_group-to-fishbone-ssas_<ID>.md` note) — process it as an ordinary `§7a` receipt per
section 3b: reuse the ID it already carries, extract into the Wiki citing that ID, log it in
`kb-registers.md`, never mint a new number for it.

## What was done

- Registered in `Outputs/kb-registers.md`'s `Processed items` table as `skipped` (informational,
  nothing to extract).
- `CLAUDE.md` replaced by version 19: section 0 gains a short pointer noting the new inbound
  channel, so a future session recognises a pre-registered `FS0000nnn`-named scan on sight rather
  than treating it as an unfiled item needing its own number.

## What was not done

No document number assigned (the notice is informational, not a business document). No Wiki
article changed — there is nothing yet to extract.

## Commit

`KB: note the group's new incoming-paper-mail process (§7a routing) in CLAUDE.md`
